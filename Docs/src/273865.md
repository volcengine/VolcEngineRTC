---
is_dir: False    # True for dir; False for doc
status: 1    # 0 for offline; 1 for online; 2 for whitelist; 4 for online but hidden in TOC
keywords: 实时音视频    # use ',' as separator
---

```mixin-react

    function SDKHubContent() {
        /**
         * @brief 将 TOS 链接从内网转化为外网
         */
        const legalizeUrl = (url) => url.replace('https://tosv.byted.org', 'https://lf-unpkg.volccdn.com');

        /**
         * @brief 根据信息返回 SDKHub 固定链接(SDK)
         */
        const fixUrl = (sdkId, versionId, fileName) => {
            if (!sdkId || !versionId || !fileName) return "";
            const link = `https://demo.volcvideo.com/exampleCenter/dynamicMarkdownManage/sourceFixedLink?queryType=sdk_version_source&reference_sdk_id=${sdkId}&reference_sdkversion_id=${versionId}&fileName=${fileName}&_sourceName=vsop`;
            return link;
        };

        /**
         * @brief normalize name
         */
        const normalizeName = (name) => name.replace(/[\s\-.\/]/g, '_');

        /**
         * @brief 根据信息返回 SDKHub 固定链接(Example)
         */
        const exampleFixUrl = (demoId, versionId, fileName) => {
            if (!demoId || !versionId || !fileName) return "";
            const link = `https://demo.volcvideo.com/exampleCenter/dynamicMarkdownManage/sourceFixedLink?queryType=example_version_source&reference_demo_id=${demoId}&reference_demoversion_id=${versionId}&fileName=${fileName}&_sourceName=vsop`;
            return link;
        };

        /**
         * @brief 生成 script 标签
         */
        const genScriptString = (src) => {
        	return `\`<script src="${legalizeUrl(src)}"></script>\``;
        }

        /**
         * @brief 获取资源链接的尾缀格式
         */
        const getPackageFormat = (url) => {
            if (url.endsWith('.min.js')) {
                return 'min.js';
            }
            return url.split('.').at(-1)
        }

        const Windows32 = 'Windows x86-32';
        const Windows64 = 'Windows x86-64';
        const MacOSX86 = 'x86';
        const MacOSARM = 'ARM';
        /**
         * @brief SDK 信息
         */
        const sdkBaseData = [
            {
                key: 'Android',
                sdkId: 41,
                sourceName: (versionName, suffix) => `VolcEngineIM_Android_${versionName}.${suffix || 'zip'}`,
            },
            {
                key: 'iOS',
                sdkId: 42,
                sourceName: (versionName, suffix) => `VolcEngineIM_iOS_${versionName}.${suffix || 'zip'}`,
            },
            {
                key: 'Windows',
                sdkId: [
                    {
                        name: Windows32,
                        id: 111,
                    },
                    {
                        name: Windows64,
                        id: 112,
                    },
                ],
                sourceName: (versionName, suffix) => `VolcEngineIM_${versionName}.${suffix || 'zip'}`,
            },
            {
                key: 'macOS',
                sdkId: [
                    {
                        name: MacOSX86,
                        id: 114,
                    },
                    {
                        name: MacOSARM,
                        id: 113,
                    },
                ],
                sourceName: (versionName, suffix) => `VolcEngineIM_macOS_${versionName}.${suffix || 'zip'}`,
            },
            {
                key: 'Web',
                sdkId: 43,
                sourceName: (versionName, suffix) => `VolcEngineIM_Web_${versionName}.${suffix || 'min.js'}`,
            },
            {
                key: '微信小程序',
                sdkId: 44,
                sourceName: (versionName, suffix) => `VolcEngineIM_Weixin_MiniApp_${versionName}.${suffix || 'min.js'}`,
            },
        ];

        /**
         * @brief UIKit 信息
         */
        const uikitBaseData = [
            {
                key: 'Android',
                sdkId: 51,
                sourceName: (versionName, suffix) => `VolcEngineIM_UIKit_Android_${versionName}.${suffix || 'zip'}`,
            },
            {
                key: 'iOS',
                sdkId: 52,
                sourceName: (versionName, suffix) => `VolcEngineIM_UIKit_iOS_${versionName}.${suffix || 'zip'}`,
            },
            {
                key: 'Web',
                sdkId: 50,
                sourceName: (versionName, suffix) => `VolcEngineIM_UIKit_Web_${versionName}.${suffix || 'tgz'}`,
            },
        ];

        /**
         * @brief QuickStart 信息
         */
        const quickstartBaseData = [
            {
                key: 'Android',
                demoId: 47,
                sourceName: (versionName, suffix) => `VolcengineIM_QuickStart_Android_${versionName}.${suffix || 'zip'}`,
            },
            {
                key: 'iOS',
                demoId: 48,
                sourceName: (versionName, suffix) => `VolcengineIM_QuickStart_iOS_${versionName}.${suffix || 'zip'}`,
            },
            {
                key: 'Windows',
                demoId: 70,
                sourceName: (versionName, suffix) => `VolcengineIM_QuickStart_Windows_${versionName}.${suffix || 'zip'}`,
            },
            {
                key: 'macOS',
                demoId: 70,
                sourceName: (versionName, suffix) => `VolcengineIM_QuickStart_macOS_${versionName}.${suffix || 'zip'}`,
            },
            {
                key: 'Web',
                demoId: 52,
                sourceName: (versionName, suffix) => `VolcEngineIM_QuickStart_Web_${versionName}.${suffix || 'zip'}`,
            },
        ];

        /**
         * @brief Experience Demo 信息
         */
        const experienceBaseData = [
            {
                key: 'Android',
                demoId: 55,
                sourceName: (versionName, suffix) => `veIMDemo ${versionName}.${suffix || 'zip'}`,
            },
            {
                key: 'Web',
                demoId: 55,
                sourceName: (versionName, suffix) => `veIMDemo ${versionName}.${suffix || 'zip'}`,
            },
        ];

        const [ versions, setVersions ] = React.useState({ sdk: {}, uikit: {}, quickstart: {}, experience: {} });
        const [ sdkData, setSdkData ] = React.useState({});
        const [ uikitData, setUIKitData ] = React.useState({});
        const [ quickstartData, setQuickstartData ] = React.useState({});
        const [ experienceData, setExperienceData ] = React.useState({});

        React.useEffect(() => {
            /** 1. 处理 SDK 的资源 */
            for(const sdk of sdkBaseData) {
                const { sdkId, sourceName, key } = sdk;
                const sdkList = [];
                console.log(sdkId, typeof sdkId)
                if (typeof sdkId !== 'number') {
                    sdkList.push(...sdkId);
                } else {
                    sdkList.push({
                        name: '',
                        id: sdkId,
                    })
                }
                for(const _sdk of sdkList) {
                    const { name: _name, id: _sdkId } = _sdk;
                    const customName = normalizeName(_name);
                    fetch(
                        `https://demo.volcvideo.com/exampleCenter/sdkManage/queryAggregationData?_sceneName=im&_sourceName=vsop&id=${_sdkId}`,
                        { mode: "cors", method: "get", headers: { "content-type": "application/json", "x-request-source": "Sandbox" } }
                    ).then(async(res) => {
                        const { Result } = await res.json();
                        const { versions } = Result[0];
                        const { package_list, name, id } = versions.find(ver => ver.status === 'published');
                        if ((package_list || []).length) {
                            const item = package_list[0];
                            const url = item.url || '';
                            const nextData = {
                                ...item,
                                versionId: id,
                                version: name,
                                url: fixUrl(_sdkId, id, item.url.split('/').at(-1)),
                                fileName: sourceName(`${customName}${customName ? '_' : ''}${name}`, getPackageFormat(url)),
                            }
                            setSdkData((preview) => {
                                return {
                                    ...preview,
                                    [key]: nextData,
                                    [customName]: nextData,
                                }
                            });
                            setVersions((preview) => {
                                return {
                                    ...preview,
                                    sdk: {
                                        ...preview.sdk,
                                        [key]: name,
                                        [customName]: name,
                                    }
                                }
                            })
                        }
                    })
                }
            }

            /** 2. 处理 UIKit 的资源 */
            for(const sdk of uikitBaseData) {
                const { sdkId, sourceName, key } = sdk;
                fetch(
                    `https://demo.volcvideo.com/exampleCenter/sdkManage/queryAggregationData?_sceneName=im&_sourceName=vsop&id=${sdkId}`,
                    { mode: "cors", method: "get", headers: { "content-type": "application/json", "x-request-source": "Sandbox" } }
                ).then(async(res) => {
                    const { Result } = await res.json();
                    const { versions } = Result[0];
                    const { package_list, name, id } = versions.find(ver => ver.status === 'published');

                    if ((package_list || []).length) {
                        const item = package_list[0];
                        const url = item.url || '';
                        const nextData = {
                            ...item,
                            versionId: id,
                            version: name,
                            url: fixUrl(sdkId, id, item.url.split('/').at(-1)),
                            fileName: sourceName(name, getPackageFormat(url)),
                        }
                        setUIKitData((preview) => {
                            return {
                                ...preview,
                                [key]: nextData,
                            }
                        });
                        setVersions((preview) => {
                            return {
                                ...preview,
                                uikit: {
                                    ...preview.uikit,
                                    [key]: name,
                                }
                            }
                        })
                    }
                })
            }

            /** 3. 处理 快速开始 Demo 的资源 */
            for(const demo of quickstartBaseData) {
                const { demoId, sourceName, key } = demo;
                fetch(
                    `https://demo.volcvideo.com/exampleCenter/demoManage/queryAggregationData?id=${demoId}`,
                    { mode: "cors", method: "get", headers: { "content-type": "application/json", "x-request-source": "Sandbox" } }
                ).then(async(res) => {
                    const { Result } = await res.json();
                    const { versions } = Result[0];
                    
                    const item = versions.find(ver => ver.status === 'published');
                    const name = item?.name || '';
                    const url = item?.resource_url || '';
                    const fileName = sourceName(name);
                    if (item) {
                        const name = item.name || '';
                        const url = item.resource_url || '';
                        const fileName = sourceName(name);
                        const nextData = {
                            version: name,
                            url: exampleFixUrl(demoId, item.id, url.split('/').at(-1)),
                            fileSize: item.file_size,
                            fileName: sourceName(name, getPackageFormat(url)),
                        }
                        setQuickstartData((preview) => {
                            return {
                                ...preview,
                                [key]: nextData,
                            }
                        });
                        setVersions((preview) => {
                            return {
                                ...preview,
                                quickstart: {
                                    ...preview.quickstart,
                                    [key]: name,
                                }
                            }
                        })
                    }
                })
            }

            /** 4. 处理 体验 Demo 的资源 */
            for(const demo of experienceBaseData) {
                const { demoId, sourceName, key } = demo;
                fetch(
                    `https://demo.volcvideo.com/exampleCenter/demoManage/queryAggregationData?id=${demoId}`,
                    { mode: "cors", method: "get", headers: { "content-type": "application/json", "x-request-source": "Sandbox" } }
                ).then(async(res) => {
                    const { Result } = await res.json();
                    const { versions } = Result[0];
                    
                    const item = versions.find(ver => ver.status === 'published');
                    const name = item?.name || '';
                    const url = item?.resource_url || '';
                    const fileName = sourceName(name);
                    if (item) {
                        const name = item.name || '';
                        const url = item.resource_url || '';
                        const reference_sdk_version = item.reference_sdk_version_list?.[0] || { 'sdk_version_name': '-' };
                        const fileName = sourceName(name);
                        const nextData = {
                            version: name,
                            url: exampleFixUrl(demoId, item.id, url.split('/').at(-1)),
                            fileSize: item.file_size,
                            fileName: sourceName(name, getPackageFormat(url)),
                            referenceSdkVersion: reference_sdk_version.sdk_version_name,
                        }
                        setExperienceData((preview) => {
                            return {
                                ...preview,
                                [key]: nextData,
                            }
                        });
                        setVersions((preview) => {
                            return {
                                ...preview,
                                experience: {
                                    ...preview.experience,
                                    [key]: name,
                                }
                            }
                        })
                    }
                })
            }
        } , []);

        return (
            <RenderMd content={
`
本文档提供即时通讯 IM 最新版本的 SDK 离线包、UIKit 组件库、快速开始 Demo 和体验 Demo 资源。当前发布的最新版本信息如下：

| | Android | iOS | Web | 微信小程序 | Windows | macOS | 
| --- | --- | --- | --- | --- | --- | --- |
| SDK 版本 | ${versions.sdk.Android || '/'} | ${versions.sdk.iOS || '/'} | ${versions.sdk.Web || '/'} | ${versions.sdk?.['微信小程序'] || '/'} | ${versions.sdk.Windows || '/'} | ${versions.sdk.macOS || '/'} |
| UIKit 版本 | ${versions.uikit.Android || '/'} | ${versions.uikit.iOS || '/'} | ${versions.uikit.Web || '/'} | ${versions.uikit?.['微信小程序'] || '/'} | / | / |
| 快速开始 Demo 版本 | ${versions.quickstart.Android || '/'} | ${versions.quickstart.iOS || '/'} | ${versions.quickstart.Web || '/'} | ${versions.quickstart?.['微信小程序'] || '/'} | ${versions.quickstart.Windows || '/'} | ${versions.quickstart.macOS || '/'} |
| 体验 Demo 版本 | ${versions.experience.Android || '/'} | ${versions.sdk.iOS || '/'} | ${versions.sdk.Web || '/'} | / | / | / |


## 下载 SDK

本章节提供离线 SDK 文件，如需通过包管理工具（如 maven、pods、npm）接入 SDK，请参看[不含 UI 集成方案](291088)或[含 UI 集成方案](837289)。

| **平台** | **SDK 文件** |
| --- | --- |
| **Android** | ${sdkData?.Android ? `<Attachment link="${sdkData?.Android.url}" name="${sdkData?.Android.fileName}" size="${sdkData?.Android.fileSize}"></Attachment>` : '获取中...'} |
| **iOS** | ${sdkData?.iOS ? `<Attachment link="${sdkData?.iOS.url}" name="${sdkData?.iOS.fileName}" size="${sdkData?.iOS.fileSize}"></Attachment>` : '获取中...'} |
| **Web** |  ${sdkData?.Web ? `<li>下载离线包:</li><Attachment link="${sdkData?.Web.url}" name="${sdkData?.Web.fileName}" size="${sdkData?.Web.fileSize}" /><li>从 CDN 引入:</li>${genScriptString(sdkData?.Web.url || '/')}` : '获取中...'} |
| **微信小程序** | ${sdkData?.['微信小程序'] ? `<Attachment link="${sdkData?.['微信小程序'].url}" name="${sdkData?.['微信小程序'].fileName}" size="${sdkData?.['微信小程序'].fileSize}"></Attachment>` : '获取中...'} |
| **Windows** | <li>${Windows32}: </li>${sdkData?.[normalizeName(Windows32)] ? `<Attachment link="${sdkData?.[normalizeName(Windows32)].url}" name="${sdkData?.[normalizeName(Windows32)].fileName}" size="${sdkData?.[normalizeName(Windows32)].fileSize}"></Attachment>` : '获取中...'}<li>${Windows64}: </li>${sdkData?.[normalizeName(Windows64)] ? `<Attachment link="${sdkData?.[normalizeName(Windows64)].url}" name="${sdkData?.[normalizeName(Windows64)].fileName}" size="${sdkData?.[normalizeName(Windows64)].fileSize}"></Attachment>` : '获取中...'} |
| **macOS** | <li>${MacOSX86} 架构: </li>${sdkData?.[normalizeName(MacOSX86)] ? `<Attachment link="${sdkData?.[normalizeName(MacOSX86)].url}" name="${sdkData?.[normalizeName(MacOSX86)].fileName}" size="${sdkData?.[normalizeName(MacOSX86)].fileSize}"></Attachment>` : '获取中...'}<li>${MacOSARM} 架构: </li>${sdkData?.[normalizeName(MacOSARM)] ? `<Attachment link="${sdkData?.[normalizeName(MacOSARM)].url}" name="${sdkData?.[normalizeName(MacOSARM)].fileName}" size="${sdkData?.[normalizeName(MacOSARM)].fileSize}"></Attachment>` : '获取中...'} |

## 下载 UIKit

IM SDK 提供 UIKit 组件库，你可以基于 UI 组件快速搭建自己的应用，使用时需同时下载 SDK 和 UIKit，集成指引参看[含 UI 集成方案](837289)。

| **平台** | **UIKit 文件** |
| --- | --- |
| **Android** | ${uikitData?.Android ? `<Attachment link="${uikitData?.Android.url}" name="${uikitData?.Android.fileName}" size="${uikitData?.Android.fileSize}"></Attachment>` : '获取中...'} |
| **iOS** | ${uikitData?.iOS ? `<Attachment link="${uikitData?.iOS.url}" name="${uikitData?.iOS.fileName}" size="${uikitData?.iOS.fileSize}"></Attachment>` : '获取中...'} |
| **Web** |  ${uikitData?.Web ? `<Attachment link="${uikitData?.Web.url}" name="${uikitData?.Web.fileName}" size="${uikitData?.Web.fileSize}"></Attachment>` : '获取中...'} |

## 下载快速开始 Demo

IM 提供示例工程文件，集成指引参看[跑通快速开始 Demo](https://www.volcengine.com/docs/6348/1009960)。

| **平台** | **工程文件** |
| --- | --- |
| **Android** | ${quickstartData?.Android ? `<Attachment link="${quickstartData?.Android.url}" name="${quickstartData?.Android.fileName}" size="${quickstartData?.Android.fileSize}"></Attachment>` : '获取中...'} |
| **iOS** | ${quickstartData?.iOS ? `<Attachment link="${quickstartData?.iOS.url}" name="${quickstartData?.iOS.fileName}" size="${quickstartData?.iOS.fileSize}"></Attachment>` : '获取中...'} |
| **Web** |  ${quickstartData?.Web ? `<Attachment link="${quickstartData?.Web.url}" name="${quickstartData?.Web.fileName}" size="${quickstartData?.Web.fileSize}"></Attachment>` : '获取中...'} |
| **Windows** | ${quickstartData?.Windows ? `<Attachment link="${quickstartData?.Windows.url}" name="${quickstartData?.Windows.fileName}" size="${quickstartData?.Windows.fileSize}"></Attachment>` : '获取中...'} |
| **macOS** | ${quickstartData?.macOS ? `<Attachment link="${quickstartData?.macOS.url}" name="${quickstartData?.macOS.fileName}" size="${quickstartData?.macOS.fileSize}"></Attachment>` : '获取中...'} |

## 下载体验 Demo

IM 提供编译好的体验 Demo，帮助你快速体验 IM 的核心功能。

| **平台** | **Demo 文件** | **备注** |
| --- | --- | --- |
| Android | <img src="https://portal.volccdn.com/obj/volcfe/cloud-universal-doc/upload_69b2c9c87d1b1ea74fc611e63eabaf91.png" width="200"></img>${experienceData?.Android ? `<br/><Attachment link="${experienceData?.Android.url}" name="${experienceData?.Android?.fileName}" size="${experienceData?.Android?.fileSize || '0'}"></Attachment>` : '获取中...'} | Demo 版本是 ${experienceData?.Android?.version || '-'}，对应的 SDK 版本是 ${experienceData?.Android?.referenceSdkVersion || '-'} |
| iOS | <img src="https://portal.volccdn.com/obj/volcfe/cloud-universal-doc/upload_5dd20a9e58eaa1e66930ef54d5e0ed8c.png" width="200"></img> | Demo 版本是 ${versions.sdk.iOS || '-'}，对应的 SDK 版本是 ${versions.sdk.iOS || '-'} |
| Web | [火山引擎即时通讯 Demo](https://demo.volcvideo.com/rtc/solution/im/login) | Demo 版本是 ${versions.sdk.Web || '-'}，对应的 SDK 版本是 ${versions.sdk.Web || '-'} |
`
            } />
        )
    }

    return <SDKHubContent />
```
